<div class="flex justify-between">
  <h1 class="font-bold text-2xl text-base-content">New quiz</h1>
  <%= link_to "Back to quizzes", quizzes_path, class: "btn btn-secondary btn-sm" %>
</div>
<div class="divider"></div>
<%= render "form", quiz: @quiz %>

