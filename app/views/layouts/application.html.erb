<!DOCTYPE html>
<html>
  <head>
    <title>DagQuickQuiz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
  <div class="navbar bg-neutral relative">
    <%= link_to "All quizzes", quizzes_path, class: "btn btn-ghost text-xl" %>
    <% if user_signed_in? %>
      <%= link_to "My quizzes", my_quizzes_user_path(current_user), class: "btn btn-ghost text-xl" %>
      <div> Welcome <%= current_user.username.blank? ? current_user.email : current_user.username %> </div> 
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-secondary text-xl ml-2" %>
      <%= link_to "New quiz", new_quiz_path, class: "btn btn-secondary text-xl absolute right-5" %>
    <% else %>
      <%= button_to "Sign in", new_user_session_path, class: "btn btn-secondary text-xl absolute right-5" %>
    <% end %>
  </div>
  <div class="container mx-auto p-2">
    <%= render 'layouts/notice' %>
    <%= yield %>
  </div>
  </body>
</html>
