<!DOCTYPE html>
<html>
  <head>
    <title>DagQuickQuiz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
  <div class="navbar bg-neutral fixed top-0 z-10">
    <%= link_to "All quizzes", quizzes_path, class: "btn btn-ghost text-xl text-neutral-content" %>
    <% if user_signed_in? %>
      <%= link_to "My quizzes", my_quizzes_user_path(current_user), class: "btn btn-ghost text-xl text-neutral-content" %>
      <div class="text-neutral-content">  Signed in: <span class="underline"><%= link_to current_user.username.blank? ? " #{current_user.email}" : "   #{current_user.username}", edit_user_registration_path %></span> </div> 
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-secondary text-xl ml-2" %>
      <%= link_to "New quiz", new_quiz_path, class: "btn btn-secondary text-xl absolute right-5" %>
    <% else %>
    <div class="absolute right-5">
      <%= link_to "New quiz", new_quiz_path, class: "btn btn-secondary text-xl mx-2" %>
      <%= link_to "Sign in", new_user_session_path, class: "btn btn-secondary text-xl mx-2" %>
    </div>
    <% end %>
  </div>
  <div class="container mx-auto p-10 pt-20">
    <%= render 'layouts/notice' %>
    <%= yield %>
  </div>
  </body>
</html>
