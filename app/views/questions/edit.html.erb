<div class="flex justify-between">
  <h1 class="font-bold text-2xl text-base-content"><%= @question.quiz.title %></h1>
  <%= link_to "Back to quiz", @question.quiz, class: "btn btn-secondary btn-sm" %>
</div>
<div class="divider"></div>
<h2 class="font-bold text-center text-xl text-base-content">Editing question</h2>

<%= render "form", question: @question %>

